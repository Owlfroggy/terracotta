FUNCTION DoTheThing;

if (not default ? IsSwimming) { default:SendMessage("you are definitely not a fish"); }

select PlayersByCondition not player ? IsSprinting;
filter ByCondition not player ? IsInGameMode{Game Mode = Spectator};

selection:SendMessage("&cslow dowb")